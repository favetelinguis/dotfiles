#!/bin/sh

set -eu

jr_bin="${HOME}/.local/bin/jr"

if [ ! -x "$jr_bin" ]; then
    printf 'jr plugin: %s is not executable\n' "$jr_bin" >&2
    exit 1
fi

if [ -n "${NNN_PIPE:-}" ]; then
    printf '+q' > "$NNN_PIPE"
fi

exec "$jr_bin"
